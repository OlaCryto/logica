---
summary: "CLI reference for `logica uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `logica uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
logica uninstall
logica uninstall --all --yes
logica uninstall --dry-run
```
