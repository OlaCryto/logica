---
summary: "CLI reference for `logica sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `logica sessions`

List stored conversation sessions.

```bash
logica sessions
logica sessions --active 120
logica sessions --json
```
